---
layout: "@layouts/MarkdownTocLayout.astro"
title: Weekly Helpdesks
subtitle: Join us to get help with your nf-core pipelines
md_github_url: https://github.com/nf-core/website/blob/main/sites/main-site/src/pages/weekly_helpdesk.mdx
---

import LocalDateTime from "@components/event/LocalDateTime.svelte";
import { nextTuesday, nextWednesday, nextThursday } from "date-fns";

# Regular time slots

We run 3 weekly helpdesk slots for different regions / time zones:

- AMER: Tuesdays 2pm ET (Tuesdays <LocalDateTime date={(() => { const d = nextTuesday(new Date()); d.setHours(14, 0); return d; })()} date_options={{ hour: "2-digit", minute: "2-digit", timeZone: "America/New_York" }} client:visible /> in your local time zone)
- APAC: Wednesdays 3pm NZDT (Wednesdays <LocalDateTime date={(() => { const d = nextWednesday(new Date()); d.setHours(15, 0); return d; })()} date_options={{ hour: "2-digit", minute: "2-digit", timeZone: "Pacific/Auckland" }} client:visible /> in your local time zone)
- EMEA: Thursdays 10am CET (Thursdays <LocalDateTime date={(() => { const d = nextThursday(new Date()); d.setHours(10, 0); return d; })()} date_options={{ hour: "2-digit", minute: "2-digit", timeZone: "Europe/Paris" }} client:visible /> in your local time zone)
