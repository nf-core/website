---
import HackathonGroup from '@components/event/HackathonGroup.astro';
import { getCollection } from 'astro:content';

let projects = await getCollection('hackathon-projects');
---

<div>
    {projects.map((project) => <HackathonGroup slug={project.slug} {...project.data} />)}
</div>
