---
const { md_github_url } = Astro.props;

const buildTimestamp = new Date().toUTCString().split(", ")[1].trim();
---

<footer class="subfooter bg-body-tertiary py-3">
    <div class="container text-center">
        {
            md_github_url && (
                <div class="row align-items-center">
                    <div class="col" />
                    <div class="col-auto">
                        <a href={md_github_url}>
                            <i class="fab fa-github" />
                            See the source code for this page
                        </a>
                    </div>
                    <div class="col text-secondary small d-none d-md-inline text-end">Built: {buildTimestamp}</div>
                </div>
            )
        }
        <slot />
    </div>
</footer>
