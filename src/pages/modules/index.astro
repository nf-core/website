---
import ListingLayout from '@layouts/ListingLayout.astro';

import components from '@public/components.json';
const modules = components.modules;
const title = 'nf-core/modules';
const description = 'A collection of software modules for Nextflow pipelines';
import pipelines from '@public/pipelines.json';
---

<ListingLayout
    title={title}
    description={description}
    input={modules}
    pipelines={pipelines}
    filter={}
    sortBy={['Alphabetical', '# Integrations', 'Last release']}
    displayStyle={[
        { name: 'grid', icon: 'fa-regular fa-grid-2' },
        { name: 'table', icon: 'fa-regular fa-bars' },
    ]}
/>
<style></style>
