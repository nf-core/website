---
import pipelines_json from '../../../public/pipelines.json';
import ListingLayout from '@layouts/ListingLayout.astro';

const pipelines = pipelines_json.remote_workflows;
const title = 'Pipelines';
const description = `We currently have ${pipelines.length} pipelines available.`;

var num_archived = 0;
var sorted_by = 0;
var display_as = 0;
---

<ListingLayout
    title={title}
    description={description}
    input={pipelines}
    filter={['Released', 'Under development', 'Archived']}
    sortBy={['Last release', 'Alphabetically', 'Stars']}
    displayStyle={['grid', 'table']}
/>

<style></style>
